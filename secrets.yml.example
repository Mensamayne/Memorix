# ⚠️ EXAMPLE FILE - Copy to secrets.yml and add your real API key
# This file is tracked in git, but secrets.yml is GITIGNORED!

memorix:
  embedding:
    openai:
      # Get your API key from: https://platform.openai.com/api-keys
      api-key: sk-proj-YOUR_OPENAI_API_KEY_HERE

